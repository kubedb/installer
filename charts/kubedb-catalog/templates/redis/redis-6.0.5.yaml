{{ if .Values.catalog.redis }}

---
apiVersion: catalog.kubedb.com/v1alpha1
kind: RedisVersion
metadata:
  name: "6.0.5"
  labels:
    {{- include "kubedb-catalog.labels" . | nindent 4 }}
spec:
  version: "6.0.5"
  db:
    image: "hremon331046/redis:6.0.5"
  exporter:
    image: "oliver006/redis_exporter:alpine"
  podSecurityPolicies:
    databasePolicyName: redis-db

{{ end }}

{{ if .Values.catalog.kafka }}
apiVersion: catalog.kubedb.com/v1alpha1
kind: KafkaVersion
metadata:
  name: '3.5.1'
  labels:
    {{- include "kubedb-catalog.labels" . | nindent 4 }}
spec:
  cruiseControl:
    image: '{{ include "catalog.registry" (merge (dict "_reg" "ghcr.io" "_repo" "kubedb") .Values) }}/cruise-control:3.5.1'
  db:
    image: '{{ include "catalog.registry" (merge (dict "_reg" "ghcr.io" "_repo" "kubedb") .Values) }}/kafka-kraft:3.5.1'
  podSecurityPolicies:
    databasePolicyName: kafka-db
  version: 3.5.1
{{ end }}

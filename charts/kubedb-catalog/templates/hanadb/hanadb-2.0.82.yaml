{{ $featureGates := .Values.featureGates }}
{{- if .Values.global }}
  {{ $featureGates = mergeOverwrite dict .Values.featureGates .Values.global.featureGates }}
{{- end }}

{{ if $featureGates.HanaDB }}

apiVersion: catalog.kubedb.com/v1alpha1
kind: HanaDBVersion
metadata:
  name: '2.0.82'
  labels:
    {{- include "kubedb-catalog.labels" . | nindent 4 }}
spec:
  db:
    image: '{{ include "image.dockerHub" (merge (dict "_repo" "saplabs/hanaexpress") $) }}:2.00.082.00.20250528.1'
  updateConstraints:
    allowlist:
    - 2.0.82
  version: 2.0.82
{{ end }}

{{- template "kubedb-catalog.refresh-versions" . }}

{{ $featureGates := .Values.featureGates }}
{{ $ubi := .Values.distro.ubi }}
{{- if .Values.global }}
  {{ $featureGates = mergeOverwrite dict .Values.featureGates .Values.global.featureGates }}
  {{ $ubi = default .Values.distro.ubi .Values.global.distro.ubi }}
{{- end }}

{{ if and $featureGates.Milvus (not (eq $ubi "operator")) }}
{{ if not (has "3.0.0" .Values.disableVersions.Milvus) }}


apiVersion: catalog.kubedb.com/v1alpha1
kind: MilvusVersion
metadata:
  name: '3.0.0'
  labels:
    {{- include "kubedb-catalog.labels" . | nindent 4 }}
spec:
  db:
    image: '{{ include "image.ghcr" (merge (dict "_repo" "appscode-images/milvus") $) }}:3.0.0'
  etcdVersion: v3.5.25
  securityContext:
    runAsUser: 1000
  version: 3.0.0
{{ end }}
{{ end }}

properties:
  catalog:
    properties:
      elasticsearch:
        type: boolean
      etcd:
        type: boolean
      memcached:
        type: boolean
      mongo:
        type: boolean
      mysql:
        type: boolean
      perconaxtradb:
        type: boolean
      pgbouncer:
        type: boolean
      postgres:
        type: boolean
      proxysql:
        type: boolean
      redis:
        type: boolean
    type: object
  dockerRegistry:
    type: string
  fullnameOverride:
    type: string
  nameOverride:
    type: string
required:
- catalog
- dockerRegistry
type: object

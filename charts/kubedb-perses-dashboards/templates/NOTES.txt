To verify that KubeDB Perses Dashboards have been installed, run:

  kubectl get persesdashboards --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "kubedb-perses-dashboards.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"

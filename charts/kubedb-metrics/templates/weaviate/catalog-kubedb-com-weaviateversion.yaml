{{ $featureGates := .Values.featureGates }}
{{- if .Values.global }}
  {{ $featureGates = mergeOverwrite dict .Values.featureGates .Values.global.featureGates }}
{{- end }}

{{ if $featureGates.Weaviate }}

apiVersion: metrics.appscode.com/v1alpha1
kind: MetricsConfiguration
metadata:
  name: catalog-kubedb-com-weaviateversion
spec:
  targetRef:
    apiVersion: catalog.kubedb.com/v1alpha1
    kind: WeaviateVersion
  metrics:
    - name: created
      help: "Unix creation timestamp"
      type: gauge
      field:
        path: .metadata.creationTimestamp
        type: DateTime
      metricValue:
        valueFromPath: .metadata.creationTimestamp

    - name: info
      help: "Weaviate versions info"
      type: gauge
      labels:
        - key: version
          valuePath: .spec.version
        - key: db_image
          valuePath: .spec.db.image
        - key: deprecated
          valuePath: .spec.deprecated
        - key: endOfLife
          valuePath: .spec.endOfLife
      metricValue:
        value: 1

{{ end }}

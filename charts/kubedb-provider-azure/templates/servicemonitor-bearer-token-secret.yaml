{{- if eq "prometheus.io/operator" ( include "monitoring.agent" . ) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-metrics-token" (include "kubedb-provider-azure.fullname" .) }}
  namespace: {{ .Release.Namespace }}
  annotations:
    kubernetes.io/service-account.name: {{ include "kubedb-provider-azure.serviceAccountName" . }}
  labels:
    {{- include "kubedb-provider-azure.labels" . | nindent 4 }}
type: kubernetes.io/service-account-token
{{- end }}

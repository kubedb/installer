apiVersion: addons.kubestash.com/v1alpha1
kind: Addon
metadata:
  name: neo4j-addon
spec:
  backupTasks:
    - driver: Neo4jAdmin
      executor: Job
      function: neo4j-backup
      name: logical-backup
      parameters:
        - name: databases
          required: false
          usage: List of databases to backup. If not specified, all databases will be backed up.
        - name: neo4jAdminArgs
          required: false
          usage: Arguments to be passed to the neo4j-admin backup command, separate by space.
          default: "--compress=true,--expand-commands,--type=FULL"
        - default: system
          name: excludeDatabases
          required: false
          usage: Comma-separated Neo4j database names to skip during backup
        - name: from
          required: false
          usage: Source Database DNS, from where the backup will take
      singleton: true
      volumeMounts:
        - mountPath: /kubestash-tmp
          name: kubestash-tmp-volume
      volumeTemplate:
        - name: kubestash-tmp-volume
          source:
            emptyDir: {}
          usage: Holds temporary files and cache.
    - driver: Restic
      executor: Job
      function: kubedbmanifest-backup
      name: manifest-backup
      parameters:
        - default: "true"
          name: enableCache
          required: false
          usage: Enable or disable caching.
        - default: /kubestash-tmp
          name: scratchDir
          required: false
          usage: Directory for temp files/cache.
      singleton: true
      volumeMounts:
        - mountPath: /kubestash-tmp
          name: kubestash-tmp-volume
      volumeTemplate:
        - name: kubestash-tmp-volume
          source:
            emptyDir: {}
          usage: Holds temp files and restic cache.
  restoreTasks:
    - driver: Neo4jAdmin
      executor: Job
      function: neo4j-restore
      name: logical-backup-restore
      parameters:
        - name: neo4jAdminArgs
          required: false
          usage: Arguments to be passed to the neo4j-admin restore command, separate by space.
          default: "--overwrite-destination=true,--expand-commands"
        - name: databases
          required: false
          usage: List of databases to restore. If not specified, all databases will be restore.
          default: "*"
        - default: system
          name: excludeDatabases
          required: false
          usage: Comma-separated Neo4j database names to skip during restore
        - name: waitTimeout
          required: false
          usage: Seconds to wait for DB ready.
          default: "300"
        - name: seedServerName
          required: false
          usage: Name of the seed server to restore from. If not specified, the restore will be done from the primary server.
        - name: remoteAliasCredentialsSecret
          required: false
          usage: Name of the Secret containing credentials for authenticating with the remote Neo4j alias during restore.
      singleton: true
      volumeMounts:
        - mountPath: /kubestash-tmp
          name: kubestash-tmp-volume
      volumeTemplate:
        - name: kubestash-tmp-volume
          source:
            emptyDir: {}
          usage: Holds temporary files and cache.
    - driver: Restic
      executor: Job
      function: kubedbmanifest-restore
      name: manifest-restore
      parameters:
        - default: "true"
          name: enableCache
          required: false
          usage: Enable or disable caching. Disabling caching may impact backup performance.
        - default: /kubestash-tmp
          name: scratchDir
          required: false
          usage: Directory for holding temporary files and restic cache.
      singleton: true
      volumeMounts:
        - mountPath: /kubestash-tmp
          name: kubestash-tmp-volume
      volumeTemplate:
        - name: kubestash-tmp-volume
          source:
            emptyDir: {}
          usage: Holds temporary files and restic cache.

apiVersion: addons.kubestash.com/v1alpha1
kind: Function
metadata:
  name: solr-restore
spec:
  args:
  - restore
  - --namespace=${namespace:=default}
  - --restoresession=${restoreSession:=}
  - --snapshot=${snapshot:=}
  image: ghcr.io/shuvokumarmondal/solr-plugin:multi-backend-v3.0_linux_amd64

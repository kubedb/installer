apiVersion: addons.kubestash.com/v1alpha1
kind: Function
metadata:
  name: solr-restore
spec:
  args:
  - restore
  - --namespace=${namespace:=default}
  - --restoresession=${restoreSession:=}
  - --snapshot=${snapshot:=}
  image: ghcr.io/kubedb/solr-plugin:v0.1.0
